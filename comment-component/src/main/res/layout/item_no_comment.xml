<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data></data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/offset_430px">

        <ImageView
            android:id="@+id/empty_iv"
            android:layout_width="@dimen/offset_76px"
            android:layout_height="@dimen/offset_76px"
            android:src="@drawable/ic_no_comment"
            app:layout_constraintBottom_toTopOf="@id/empty_tv"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_chainStyle="packed" />

        <TextView
            android:id="@+id/empty_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/offset_20px"
            android:textColor="@color/color_67748c"
            android:textSize="@dimen/font_size_sp_14"
            android:text="@string/comment_no_comment"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/empty_iv" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
