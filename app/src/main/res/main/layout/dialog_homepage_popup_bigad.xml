<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardView_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:elevation="@dimen/offset_0dp"
        app:cardCornerRadius="@dimen/offset_5dp">
        <ImageView
            android:id="@+id/dialog_popup_bigad_img"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:maxWidth="9720dp"
            android:maxHeight="9920dp"
            android:scaleType="centerInside"
            android:src="@drawable/default_image"
            />

    </androidx.cardview.widget.CardView>

    <ImageView
        android:id="@+id/dialog_popup_bigad_close"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginTop="20dp"
        android:scaleType="fitXY"
        android:src="@drawable/popupad_close"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cardView_bg" />

    <TextView
        android:id="@+id/adv_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/offset_5dp"
        android:layout_marginBottom="@dimen/offset_5dp"
        android:background="@drawable/adcover"
        android:gravity="center"
        android:shadowColor="@color/black_color"
        android:shadowRadius="5"
        android:textColor="@color/white"
        android:textSize="@dimen/font_size_9sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/cardView_bg"
        app:layout_constraintLeft_toLeftOf="@id/cardView_bg" />

</androidx.constraintlayout.widget.ConstraintLayout>